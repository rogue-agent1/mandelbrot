# mandelbrot
Mandelbrot set ASCII/Unicode renderer with zoom. Zero dependencies.
## Usage
```bash
python3 mandelbrot.py
python3 mandelbrot.py --zoom 10 --cx -0.75 --cy 0.1 -i 500
python3 mandelbrot.py --style ascii -W 120 -H 60
```
